---
// Base for <body> element
type Props = {
  classNames?: string;
};

const { classNames } = Astro.props;
---

<body
  class:list={[
    'antialiased font-sans flex flex-col min-h-screen mx-auto max-w-3xl px-2 lg:px-0 prose prose-a:text-secondary-content prose-a:no-underline prose-a:hover:underline',
    classNames,
  ]}
>
  <slot />
</body>
