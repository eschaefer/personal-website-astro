---
type Props = {
  date: Date;
};

const { date } = Astro.props;
---

<time
  datetime={date.toISOString()}
  class="text-base-content/80 text-sm font-thin"
>
  {
    date.toLocaleDateString('en-us', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
